# Jack Answers

Symfony 8 app running in Docker with Stimulus and Turbo.

## Requirements

- Docker
- Docker Compose

## Start

```sh
docker compose up --build
```

Open http://localhost:8000.

The Docker stack includes one Symfony app service.
